name: ethio_shop
description: The ETHIO🛍 Adaptive Marketplace application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 🎨 Icons
  cupertino_icons: ^1.0.6

  # 🔑 Firebase Packages
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.2
  cloud_firestore: ^4.13.5
  firebase_storage: ^11.5.6

  # 🗺 Maps & Location
  google_maps_flutter: ^2.5.0
  geolocator: ^10.1.0

  # 🛠 Utilities (Fixes your 'URI doesn't exist' errors)
  connectivity_plus: ^5.0.2  # Required for checking internet
  lottie: ^3.1.0             # Required for animations
  
  # 🖼 Media
  image_picker: ^1.0.4
  cached_network_image: ^3.3.0
  intl: ^0.18.1
  url_launcher: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  # ⚠️ Make sure to create these folders in your project root!
  assets:
    - assets/lottie/
    - assets/icons/
    - assets/branding/    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  # 🖼 Assets Section
  # Make sure you create an 'assets' folder in your project root if you use this!
  # assets:
  #   - assets/images/
